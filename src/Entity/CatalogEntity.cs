namespace WorldMapStudio;

/// <summary>
/// An entity not tied to a scene, loaded and unloaded on demand by an external system
/// (e.g. quests, editable assets).
/// </summary>
public abstract class CatalogEntity : Entity
{
}
